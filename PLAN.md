# 公主迷宮探險：草原故事與神秘森林實作計畫

## 1. 現有專案分析

- 專案使用 Phaser 3、TypeScript、Vite、Arcade Physics，沒有其他遊戲框架。`src/config/GameConfig.ts` 設定地圖與關卡；`src/world/MapGenerator.ts` 以主路、岔路及 BFS 產生並驗證迷宮。起點是隨機角落，終點是對角城堡。
- `src/input/InputController.ts` 處理拖曳與鍵盤移動；`src/entities/Princess.ts` 與 `src/world/Collision.ts` 處理角色及靜態障礙碰撞；`src/ui/Minimap.ts` 記錄探索範圍；`src/world/Goal.ts` 與 `src/scenes/GameScene.ts` 處理到達城堡、過關及重啟。原有五關均為草原且難度參數逐關提升，符合需求描述。
- 原本的圖像由 Phaser Graphics 即時產生，沒有素材檔、道具狀態、任務提示或自動測試。需要實機確認的事項是小朋友辨識圖示的程度、觸控手感與低階手機效能。

## 2. 功能設計

- 關卡標籤置於左上、地圖按鈕置於右上，圖像任務順序獨立排列於下方；完成的圖示亮起。提早碰到城堡時顯示鎖或藤蔓晃動，並閃爍下一個任務圖示。所有互動都由靠近物件觸發，不增加點擊或文字閱讀負擔。
- 第 1 關只到城堡；第 2 關找到圓潤、沒有明顯腳但會跳著跟隨的兔子，再到城堡；第 3 關拾取鑰匙後城堡開門；第 4 關以任意順序在不同岔路讓粉色與藍色花綻放後解除藤蔓，提示呈現「花 + 花 → 城堡」；第 5 關到發光樹取得葉子，再到城堡。
- 第 5 關取得的葉子只透過關卡重啟資料帶到第 6 關，不建立背包或永久儲存。進入森林及接近小精靈時以葉子圖像回應劇情。直接用測試網址進第 6 關時仍可完成任務。
- 第 6 關依序遇見小精靈、找袋子、交還小精靈、一起進城堡。小精靈有翅膀與尖耳朵，交還袋子後持續發出柔和光芒，沿公主走過的路保持距離飛著跟隨，抵達城堡時要在公主附近。森林由白天在遇見小精靈後轉黃昏、交還袋子後轉柔和夜晚；不增加限時壓力。此關不設魔法按鈕或冷卻。地圖提示魔法與泡泡海洋留待未來討論。

## 3. 技術實作方案

- 在 `GameConfig.ts` 為六關加入 `theme`、`quest`，保留既有迷宮參數與生成方式。`GameScene.ts` 選擇場景色彩、裝飾與森林明暗；`Collision.ts` 依場景選用相同尺寸和碰撞範圍的障礙圖像；`Minimap.ts` 切換色盤。`main.ts` 負責 Phaser 啟動設定，避免設定檔與場景循環匯入。
- 新增 `QuestPlacement.ts`，從已通過 BFS 的分岔路可走位置選任務物件，優先遠離起點與城堡、避開主路與彼此重疊，並讓兩朵花位於不同分岔路；`ArtAlignment.ts` 校正可見圖像在道具格與提示格的中心，城堡改為三塔童話造型；`QuestController.ts` 管理任務狀態與同伴跟隨；`QuestHud.ts` 呈現等距圖示與回饋。`Goal.ts` 在任務未完成時阻止過關。
- 地圖每次仍隨機生成；放置任務點只從同一連通的可走區域選取。小精靈光芒只使用少量半透明圓形及固定螢幕色罩，不建立逐格光照或新增套件；手機觸控的直向固定搖桿在下方中央、橫向固定在左下角，電腦滑鼠沿用任意非 UI 位置操作的浮動搖桿。
- 更新 `README.md` 說明六關玩法、測試網址與操作；不新增套件、E2E 測試或測試框架。

## 4. 分階段實作計畫

1. **關卡資料與任務位置**：加入第六關、場景與任務設定，確認每個任務點可達且彼此不重疊。
2. **草原任務與提示**：加入兔子、鑰匙、兩朵花、發光樹及圖像進度；確認各關未完成任務不能過關，完成後可過關。
3. **森林故事與光芒**：加入森林外觀、小精靈袋子流程、葉子銜接、日夜轉換與交還後持續發光；確認跟隨時和公主保持間距。
4. **驗證與文件**：建置通過、抽樣隨機地圖、檢查手機直橫式版面與瀏覽器錯誤，更新說明文件。

## 5. 驗證計畫

- 原專案沒有測試腳本；`npm run build` 可驗證 TypeScript 與正式打包。用一次性檢查腳本抽樣六關隨機地圖，逐一檢查迷宮連通及任務點位可達、不重疊。
- 手動檢查任務順序、提早到城堡的提示、兔子跟隨、花朵各自完成、鑰匙與葉子狀態、小精靈交還條件、發光與跟隨間距、地圖暫停及畫面尺寸變化。以手機直式、橫式和實機 FPS 補足固定搖桿的視覺與觸控驗證；不預設 E2E。

## 6. 風險與待決策事項

- 格子 BFS 證明地圖上的路徑連通；實際角色碰撞、任務物件可辨識度及迷宮長度仍須在手機上玩測。若實機出現狹窄或過長路線，優先調整放置分數、碰撞半徑及關卡參數，不重寫地圖生成。
- 目前採本次遊玩葉子狀態、交還袋子後小精靈持續發光及六關完結。未來若要跨重新整理保存、加入地圖提示魔法或新地形，再另外決定持久化、介面與關卡資料擴充方式。
